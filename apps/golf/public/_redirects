# Legacy URLs are generated HTML redirects in every Vite build, including on
# GitHub Pages. They preserve the course, view query and deployment base without
# host-specific rewrite rules. Do not rewrite them to a historical renderer.
#
# No catch-all: missing course/model assets must remain real 404 responses.
